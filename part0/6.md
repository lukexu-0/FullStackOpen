```md
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User types a note into the text field
    Note right of browser: User clicks "Save"
    Note right of browser: spa.js intercepts submit (preventDefault)
    Note right of browser: Browser updates its local state and redraws notes

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    Note right of browser: Payload: { content, date } (JSON)\nHeader: Content-Type: application/json
    activate server
    Note left of server: Server stores the new note
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No redirect, no page reload, no further HTTP requests